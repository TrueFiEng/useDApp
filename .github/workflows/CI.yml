name: CI
on:
  pull_request:
  push:
    branches:
      - master
      - rzadp/playwrights
jobs:
  # test:
  #   strategy:
  #     matrix:
  #       node: ['14.x', '16.x']
  #       os: [ubuntu-latest]
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #   - uses: actions/checkout@v2
  #   - name: Use Node.js
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: ${{ matrix.node }}
  #   - run: npm i -g pnpm@6.20.3
  #   - run: pnpm install --frozen-lockfile
  #   - run: yarn lint
  #   - run: yarn build
  #   - run: yarn test
  test-example:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'
      - run: npm i -g pnpm@6.20.3
      - run: pnpm install --frozen-lockfile
      - name: Install Playwright dependencies
        run: pnpm playwright install-deps
        working-directory: packages/example
      - name: Install xvfb dependencies
        run: sudo apt-get install -y libgtk2.0-0 libgtk-3-0 libgbm-dev libnotify-dev libgconf-2-4 libnss3 libxss1 libasound2 libxtst6 xauth
      - name: Build core
        run: yarn build
        working-directory: packages/core
      - name: Build coingecko
        run: yarn build
        working-directory: packages/coingecko
      - name: Unzip Chrome Metamask extension
        run: unzip metamask-chrome-10.12.4.zip -d ./metamask-chrome-10.12.4
        working-directory: packages/example/playwright/metamask
      - name: Run playwright
        run: |
          set -e
          yarn start &
          xvfb-run --auto-servernum --server-args="-screen 0 1280x960x24" -- yarn test:playwright
        env:
          MAINNET_URL: ${{ secrets.MAINNET_PROVIDER_URL }}
        working-directory: packages/example
