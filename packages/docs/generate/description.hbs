{{!--
The override adds a markdown text prefix because otherwise,
description is wrapped in <p></p> and markdown links are not properly recognized inside.

Additionaly, @link links are NOT processed,
because it does not work properly for cross-page links
 --}}
{{#if description}}**Description**: {{{description}}}

{{/if}}
