# Next.js integration

To set up usedapp with next.js just wrap it with `DAppProvider` like in default configuration.

```tsx
import { DAppProvider, Mainnet } from '@usedapp/core'

const config = {
  readOnlyChainId: Mainnet.chainId,
  readOnlyUrls: {
    [Mainnet.chainId]: 'https://mainnet.infura.io/v3/acf40056939949ef8c218d3cab625a7b',
  },
}

function MyApp({ Component, pageProps }) {
  return (
    <DAppProvider config={config}>
      <Component {...pageProps} />
    </DAppProvider>)
}

export default MyApp
```

**Example**

Complex example of using is [here](https://github.com/ChangoMan/nextjs-ethereum-starter/tree/f1911719b1cd90b4959e1777307decc0ce12c7db).
